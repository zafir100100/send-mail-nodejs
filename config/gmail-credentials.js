const GmailCredentials = Object.freeze({
    SERVICE: 'gmail',
    USERNAME: 'YOUR_FULL_GMAIL_ADDRESS',
    APP_PASSWORD: 'YOUR_GMAIL_APP_PASSWORD' //you must enable 2 step verifcation and generate an app password after that. Seee https://support.google.com/accounts/answer/185833?hl=en
});

module.exports = GmailCredentials;